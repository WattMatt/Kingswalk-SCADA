# Edge Gateway

Phase 2. This directory will contain the on-site Modbus TCP polling agent (pymodbus).

See spec/SPEC.md §B.3.2 for architecture requirements.
MONITORING ONLY — no Modbus write function codes (FC06/FC16) ever.
